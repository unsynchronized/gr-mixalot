This is the mixalot-write-a-block package meant as a guide to building
out-of-tree packages. To use the mixalot blocks, the Python namespaces
is in 'mixalot', which is imported as:

    import gnuradio.mixalot as mixalot

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(mixalot)
